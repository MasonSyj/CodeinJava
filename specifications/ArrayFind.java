package specification;

public class ArrayFind {
/**
*	Find a value in an array
*	@param array the array to search, requires: val occurs exactly only once in array
*	@param value that to search for in the array
*	@return returns index i such that array[i] = val
*/
	
	public static int find(int[] array, int val);
	
/**
*	Check if a word is a Palindrom
* 	Palindrom is a sequence of characters
*	that needs the same forwards and backwords
*	@param word word to check, must only contain alphanumeric characters
*	@return true if and if only word is a palindrom
*/
	
	public static boolean isPalindrom(String word);
	
	public static void main(String[] args) {
		
	}
}